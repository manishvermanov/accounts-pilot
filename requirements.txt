# --- core ---
pydantic>=2.6
pydantic[email]>=2.6        # EmailStr needs email-validator
pydantic-settings>=2.2
typer>=0.12
rich>=13.7
python-dotenv>=1.0

# --- browser automation ---
playwright>=1.44
# IMPORTANT: after `pip install -r requirements.txt`, also run:  python -m playwright install chromium
# AgentQL: local Playwright element-resolution (actions stay local). `pip install agentql`
# agentql
# CloakBrowser is installed on demand (stealth swap-in). Uncomment to pin:
# cloakbrowser

# --- photos (download → EXIF-orient → landscape white-pad → resize for OTA upload) ---
Pillow>=10.0

# --- gate handlers (OTP / CAPTCHA) ---
httpx>=0.27

# --- web product (dashboard API + server) ---
fastapi>=0.110
uvicorn>=0.29

# --- LLM page-filler (Azure OpenAI) ---
openai>=1.30

# --- dev ---
pytest>=8.2

# --- MIS Postgres fallback (Metabase path uses stdlib urllib) ---
psycopg[binary]>=3.1
